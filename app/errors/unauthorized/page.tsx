import { UnauthorizedCard } from "./components";

const UnauthorizedPage = () => {
  return (
    <main className="flex h-full min-h-screen w-full flex-col items-center justify-center p-5">
      <UnauthorizedCard />
    </main>
  );
};

export default UnauthorizedPage;
